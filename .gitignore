# 자동 생성물 — 지워도 되고, 코드가 필요할 때 다시 만든다
# nullify.db: SQLite(스캔·이력·계정), 런타임 생성
nullify.db
# sample_repo: 수정 PR 데모용 git 레포. github_pr 가 자동 생성(레포 안 레포 방지)
sample_repo/

# 파이썬 캐시
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# 로컬 환경/시크릿 — 절대 커밋 금지 (ANTHROPIC_API_KEY 등)
.env
.env.*

# 에디터/OS
.vscode/
.idea/
.DS_Store
Thumbs.db
clones/
